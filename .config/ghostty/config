# Ghostty issues to sort out:
# - Get dynamic cursor types working (without blinking).
# - quit-after-last-window-closed=true (which is set automatically by -e)
#   doesn't seem to work. If I close the terminal, the old ghostty process
#   lingers.

gtk-titlebar = false
command = fish

font-family = Inconsolata

cursor-style-blink = false
shell-integration-features = no-cursor,no-title

font-feature = -calt
font-feature = -liga
font-feature = -dlig
# My version of Inconsolata does glyph composition to render `` differently from `.
# Turn that off.
font-feature = -ccmp

theme = cespare

mouse-hide-while-typing = true
